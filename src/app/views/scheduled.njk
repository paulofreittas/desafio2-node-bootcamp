{% extends "_layouts/default.njk" %}

{% block body %}
    <div class="content">
        <strong>Olá, {{ user.name }}</strong>
        <p>Seus atendimentos para hoje:</p>

        <ul class="providers">
            {% for provider in appointments %}
                <li class="provider">
                    <div>
                        <img src="/files/{{ provider.user.avatar }}" alt="">
                        <strong>{{ provider.user.name }}</strong>
                        <strong>{{ provider.date | date("HH:mm") }} Horas</strong>
                    </div>
                    
                </li>
            {% endfor %}
        </ul>

        <a href="/app/logout" class="logout">Sair</a>
    </div>
{% endblock %}